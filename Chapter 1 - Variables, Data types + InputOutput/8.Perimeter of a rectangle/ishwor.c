#include<stdio.h>
int main(){
    int l,b;
    printf("Enter a length of a rectangle :");
    scanf("%d",&l);
    printf("Enter a breadth of a rectangle :");
    scanf("%d",&l);
    
    printf("Perimeter of a rectangle is : %d", 2*((l*b)/100));
    return 0;
}